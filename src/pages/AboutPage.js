import React from 'react';
import About from '../components/About';  // 确保路径正确对应到你的 Register 组件

const AboutPage = () => {
    return (
        <div className="register-page-container">
            <About />
        </div>
    );
};

export default AboutPage;
