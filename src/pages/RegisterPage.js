import React from 'react';
import Register from '../components/Register';  // 确保路径正确对应到你的 Register 组件

const RegisterPage = () => {
    return (
        <div className="register-page-container">
            <Register />
        </div>
    );
};

export default RegisterPage;
